# modest-css

A simple starter theme using Skeleton CSS and turning it into a SASS build with Gulp.

Run gulp as the default task to compile sass and watch for changes/reload on change.

To do list:

<ul>
<li>Remove demo styles</li>
<li>Improve gitignore</li>
</ul>
